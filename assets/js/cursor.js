/* THE CONTEXT WINDOW — cursor.js
   STATUS: not in use.
   The terminal bar cursor blink is handled entirely via CSS
   (@keyframes blink in base.css). This file is reserved in case
   a JS-driven effect (e.g. typewriter text reveal) is added later.
*/
